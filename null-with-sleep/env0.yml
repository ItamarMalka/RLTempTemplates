version: 2
shell: bash

deploy:
  steps:
    setupVariables:
      after: 
      - name: 'complex command run'
        run: |
          echo $SHELL && ls || ./script.sh
    terraformInit:
      before:
      - name: 'RGM'
        run: echo 'hello'
      after:
      - name: 'RGM123'
        run: |
          echo 'hello' && shopt -s lastpipe
